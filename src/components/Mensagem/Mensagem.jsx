
const Mensagem = (props) => {
    // const {conteudo} = props
    return (
        <div style={{margin: '20px'}}>
            <h2>{`Olá, ${props.conteudo}`}</h2>
        </div>

    )
  }
  
  export default Mensagem

